import Image from "next/image";
import { Callout, Cards } from "nextra/components";
import { FaGithub } from "react-icons/fa";
import arch from "./pics/klang-arch.svg";
import ast from "./pics/klang-ast.svg";

# Klang

<Callout type="warning" emoji="⚠️">
  This page is currently under construction.
</Callout>

Klang is a domain-specific language (DSL) for programming robots.

<Cards>
  <Cards.Card
    icon={<FaGithub />}
    title="Github"
    href="https://github.com/kscalelabs/klang"
    description="Contribute to our open-source projects"
  />
</Cards>

Klang is built into K-Scale OS, and is designed to be a simple and flexible way to get started building robot applications.

## Neural Interpreter

Below is a diagram showing the architecture of the Klang neural interpreter.

<Image alt="Klang Neural Interpreter" src={arch} style={{ width: "100%", marginTop: "1em" }} />

## Abstract Syntax Tree

Here is a sample abstract syntax tree (AST) for a Klang program:

<Image alt="Klang AST" src={ast} style={{ width: "100%", marginTop: "1em" }}/>
