# Sim2Real

Although we have a policy trained in simulation, we want to deploy our policy in the real world. This isn't a simple problem --- we must account for many sources of error, such as matching motor controls to simualtion, mimicking real world physics, and matching sensor data.